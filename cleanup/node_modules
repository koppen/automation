#!/bin/bash

# Free up diskspace by removing all node modules in my Projects directories.
#
# A bit heavyhanded, agreed, but they are fairly easy to recreate.

set -e

df -h /

find -E ~/Projects/ -regex '.*/(node_modules)' -d -print -exec rm -rf {} \;

df -h /
